
export default class AccountNumber {
    lines: string[];


    constructor(lines: string[]) {
        this.lines = lines;
    }

    // get rawDigits
    // parseRawDigits
    // setAccountNumber (or soemthing)
}